1. 刚刚完成系统初始化。
2020-2-20
问题记录
1. 在添加学生信息之后如果再次添加信息会出现问题，目前已知的是第二次添加信息时只添加一条记录会出错。
2. 在添加学生信息之后如果删除最后一条信息时也会出现错误，问题出在调用delete_student_node时返回值为空，
在返回之前打印head时显示已经删除了最后一条信息，没有错误，但是返回之后却变成了NULL。
2020-2-21
解决问题
1. 找到问题所在了，在main.c文件里add_student函数，s_temp->prev->next-> = s_temp;这一句写错了，
本来是要连接前后两个节点，却变成了s_temp = s_temp，导致新添加的前后为NULL。
2020-2-22
基本上已经完成，目前测试暂未发现问题，